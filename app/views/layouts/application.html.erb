<!DOCTYPE html>
<html>
<head>
  <title>MovieArchitect</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!--Import Google Icon Font-->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.1/css/materialize.min.css">
  <!--Let browser know website is optimized for mobile-->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <!-- Compiled and minified JavaScript -->
  <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.1/js/materialize.min.js"></script>
</head>
<body>
<header>
  <div class="container">
    <div class="row">
      <div class="col s3">
        <div class="logo">
          <h1>
            <%= link_to "Url Shortener", root_path %>
          </h1>
        </div>
      </div>
     
      <div class="col s4 right-align nav-link">
        <% if user_signed_in? %>
        <% name = current_user.email.split("@")[0] %>
        Hello <strong><%= name %></strong>

        <%= link_to "Logout", logout_path, method: :delete, :class => 'navbar-link'  %>
        <% else %>
          <%= link_to "Sign up", signup_path, :class => 'navbar-link'  %>
          <%= link_to "Login", sessions_new_path, :class => 'navbar-link'  %>
        <% end %>
      </div>
    </div>
  </div>
</header>
<section>
<div class="container">
  <div class="row">
    <div class="col s12">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col s12">
      <%= yield %>
    </div>
  </div>
</div>
</section>
</body>
</html>
